<form method="post" style="display: inline-block" action="{{ path('app_carte_bancaire.delete', {'id': carte.id}) }}" onsubmit="return confirm('Êtes vous sûr(e) de vouloir supprimer cette carte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte.id) }}">
    <button class="btn">Supprimer</button>
</form>
