{% extends 'base.html.twig' %}

{% block title %}
    Ajouter une carte bancaire
{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <input type="submit" value="Valider !" />
    {{ form_end(form) }}

    <a href="{{ path('app_carte_bancaire') }}">Aller à la liste des cartes</a>
{% endblock %}